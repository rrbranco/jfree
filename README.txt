JFree is an ARM heap analyzer, created to help Linux for ARM developers to test code.

It uses the ARM Stack Unwinder from Michael McTernan with some additions like support to IWMMX instructions.

This is the first public version, so we call it v0.01.

The authors are:
	Julio Auto 			 <julio *noSPAM* julioauto.com>
	Rodrigo Rubira Branco (BSDaemon) <rodrigo *noSPAM* kernelhacking.com>
